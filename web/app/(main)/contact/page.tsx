import { Heading } from '@/components/heading/heading';

export const metadata = {
	title: 'Contact with Library',
	openGraph: {
		title: 'Contact with Library',
	},
};

const ContactPage = () => {
	return (
		<Heading tag="h1" size="large">
			Contact with Library
		</Heading>
	);
};

export default ContactPage;
